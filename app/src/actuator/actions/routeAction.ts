//WORKING ON IT ...

// import { BasicClause } from "../../clauses/BasicClause";
// import { Clause } from "../../clauses/Clause";
// import RelationAction from "./RelationAction";

// export function basicToAction(clause: BasicClause, topLevel: Clause) {

//     // relations (multi arg predicates) except for 'of' 
//     if (clause.args.length > 1 && clause.predicate.root !== 'of') {

//         return new RelationAction(
//             topLevel,
//             clause.predicate,
//             clause.args.map(x => this.lookup(x, context)),
//             clause.negated)
//     }





// }