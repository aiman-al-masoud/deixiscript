Resolve anaphora within the same sentence


pass down the clause of a previously converted ast to the toClause() of the next ast.

use Enviro to get a Map?




PROBLEMS:

And has an erratic behavior when it comes to theme-rheme, theme is less than it should be 

BasicClause didn't care about theme-rheme in and() method

Neither does Imply 


PROPOSED SOLUTION:

remove list inner structure from And, just rely on two clauses not a list. 
And started out as as "ListClause", now it's not a list anymore it's an And.

